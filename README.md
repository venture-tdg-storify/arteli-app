# Arteli

Frontend v2

## Enable pnpm

To enable pnpm package manager on a  run:

### `corepack enable pnpm`

## Available Scripts

In the project directory, you can run:

### .env.local
```sh
VITE_SEGMENT_API_KEY=vlbU3pS5w1JnBO5aNWyGWtfLbCiwJvuC # sends data to local workspace
VITE_SEGMENT_API_KEY= #disables segment for local env, PREFERRED
```

### `pnpm start`

Runs the app in the development mode.\
Open [http://localhost:3000](http://localhost:3000) to view it in the browser.

The page will reload if you make edits.\
You will also see any lint errors in the console.

### `pnpm serve`

Bundles the app and runs the build bundle (no hot loading).\
Open [http://localhost:3000](http://localhost:3000) to view it in the browser.

### `pnpm test`

Launches the test runner in the interactive watch mode.\

### `pnpm test run`

Launches the test runner once and reports results.

## Packages

### React Router v6
