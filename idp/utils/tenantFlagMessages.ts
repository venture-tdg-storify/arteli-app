import { defineMessages } from 'react-intl'
import { TenantFlags } from '$/api/system'

export const tenantFlagMessages = defineMessages({
  [TenantFlags.None]: { defaultMessage: 'None', id: '450Fty' }
})
